# After the API is on Vercel, change this (or set the same key in the
# frontend Vercel project's Environment Variables — that overrides this file).
VITE_API_PROXY_TARGET=https://dearte.onrender.com/api
